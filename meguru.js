(()=>{const filters=['',...new Set(ENTRIES.map(e=>e.category))];const journal=document.querySelector('#journal'),reader=document.querySelector('#reader');document.querySelector('#journal-filters').innerHTML=filters.map(c=>`<button type="button" data-filter="${c}" aria-pressed="${c===''}">${c||'すべて'}</button>`).join('');function list(category=''){document.querySelector('#journal-list').innerHTML=ENTRIES.filter(e=>!category||e.category===category).map(e=>`<a class="journal-item" href="#entry-${e.id}"><span class="number">${String(ENTRIES.indexOf(e)+1).padStart(2,'0')}</span><p class="entry-meta">${e.category}<span>見本記事</span></p><h3>${e.title}</h3><p>${e.intro}</p><span class="read-link">記録を読む <b>↗</b></span></a>`).join('')}document.querySelector('#journal-filters').addEventListener('click',e=>{let b=e.target.closest('button');if(!b)return;document.querySelectorAll('[data-filter]').forEach(x=>x.setAttribute('aria-pressed',x===b));list(b.dataset.filter)});function route(){let e=ENTRIES.find(e=>location.hash==='#entry-'+e.id);journal.hidden=!!e;reader.hidden=!e;if(e){reader.innerHTML=`<a class="back-link" href="#entries">← 記録一覧へ</a><header class="reader-head"><p class="entry-meta">${e.category} <span>見本記事</span></p><h1>${e.title}</h1><p>掲載イメージを確認するための見本原稿です。</p></header>${e.photo?'<img class="reader-photo" src="assets/kyoto.jpg" alt="見本写真：京都・二年坂" width="2200" height="1467">':''}<div class="reader-prose">${e.body.map(p=>`<p>${p}</p>`).join('')}</div><div class="reader-bottom"><a class="back-link" href="#entries">← 日々の記録に戻る</a></div>`;document.title=e.title+' — 巡る';window.scrollTo(0,0);reader.focus({preventScroll:true})}else{document.title='巡る — 歴史する';if(location.hash==='#entries')document.querySelector('#entries').scrollIntoView()}}list();window.addEventListener('hashchange',route);route()})();
